# 배운점
- 피곤해서인지 byte랑 bit랑 자꾸 헷갈린다... 4 M * 8 bit 를 4 M * 8 byte로 착각해서 용량 넘친다고 생각함.
- 자바에 BitSet이라는 자료구조가 있음.
  - 생성자에 넘기는 수로 비트 개수 정하고
  - set()으로 해당 위치 비트에 1 설정하고
  - get()으로 해당 위치 비트가 1인지 boolean으로 리턴
  - clear()로 0으로 만드는 것
- 근데 나는 해당 자료구조를 몰라서 그냥 32개의 bit를 묶은 int를 활용함.
